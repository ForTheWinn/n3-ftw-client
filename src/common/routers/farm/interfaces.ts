
export interface IFarmLPToken {
  name: string;
  tokenId: string;
  tokenA: string;
  tokenB: string;
  tokenAAmount: string; // formatted
  tokenBAmount: string; // formatted
  sharesPercentage: string; // formatted
}
